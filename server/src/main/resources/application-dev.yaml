server:
  port:8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring-auth
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
